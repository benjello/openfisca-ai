# Config

- **`countries/`** — One YAML per country (committed). Structure, repo URLs, conventions.
- **`user.example.yaml`** — Template for your local paths. Copy to **`user.yaml`** and fill in; `user.yaml` is gitignored and overrides paths in `countries/*.yaml`.

See **`countries/README.md`** for setup (Tunisia first, then more countries).
